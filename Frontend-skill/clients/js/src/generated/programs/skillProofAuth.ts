/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertIsInstructionWithAccounts,
  containsBytes,
  extendClient,
  fixEncoderSize,
  getBytesEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_ACCOUNT,
  SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_INSTRUCTION,
  SOLANA_ERROR__PROGRAM_CLIENTS__UNRECOGNIZED_INSTRUCTION_TYPE,
  SolanaError,
  type Address,
  type ClientWithRpc,
  type ClientWithTransactionPlanning,
  type ClientWithTransactionSending,
  type GetAccountInfoApi,
  type GetMultipleAccountsApi,
  type Instruction,
  type InstructionWithData,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  addSelfFetchFunctions,
  addSelfPlanAndSendFunctions,
  type SelfFetchFunctions,
  type SelfPlanAndSendFunctions,
} from "@solana/program-client-core";
import {
  getUserProfileCodec,
  type UserProfile,
  type UserProfileArgs,
} from "../accounts";
import {
  getAddSkillInstruction,
  getCloseProfileInstruction,
  getInitializeProfileInstruction,
  getReadProfileInstruction,
  getUpdateProfileInstruction,
  parseAddSkillInstruction,
  parseCloseProfileInstruction,
  parseInitializeProfileInstruction,
  parseReadProfileInstruction,
  parseUpdateProfileInstruction,
  type AddSkillInput,
  type CloseProfileInput,
  type InitializeProfileInput,
  type ParsedAddSkillInstruction,
  type ParsedCloseProfileInstruction,
  type ParsedInitializeProfileInstruction,
  type ParsedReadProfileInstruction,
  type ParsedUpdateProfileInstruction,
  type ReadProfileInput,
  type UpdateProfileInput,
} from "../instructions";

export const SKILL_PROOF_AUTH_PROGRAM_ADDRESS =
  "<GggskLBZBfEWv2h27kfGr3vsSniUFyCXCvDzvsMR4T3C>" as Address<"<GggskLBZBfEWv2h27kfGr3vsSniUFyCXCvDzvsMR4T3C>">;

export enum SkillProofAuthAccount {
  UserProfile,
}

export function identifySkillProofAuthAccount(
  account: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): SkillProofAuthAccount {
  const data = "data" in account ? account.data : account;
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([32, 37, 119, 205, 179, 180, 13, 194]),
      ),
      0,
    )
  ) {
    return SkillProofAuthAccount.UserProfile;
  }
  throw new SolanaError(
    SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_ACCOUNT,
    { accountData: data, programName: "skillProofAuth" },
  );
}

export enum SkillProofAuthInstruction {
  InitializeProfile,
  ReadProfile,
  UpdateProfile,
  AddSkill,
  CloseProfile,
}

export function identifySkillProofAuthInstruction(
  instruction: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): SkillProofAuthInstruction {
  const data = "data" in instruction ? instruction.data : instruction;
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([32, 145, 77, 213, 58, 39, 251, 234]),
      ),
      0,
    )
  ) {
    return SkillProofAuthInstruction.InitializeProfile;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([221, 127, 18, 130, 112, 49, 200, 247]),
      ),
      0,
    )
  ) {
    return SkillProofAuthInstruction.ReadProfile;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([98, 67, 99, 206, 86, 115, 175, 1]),
      ),
      0,
    )
  ) {
    return SkillProofAuthInstruction.UpdateProfile;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([193, 213, 92, 116, 27, 180, 153, 6]),
      ),
      0,
    )
  ) {
    return SkillProofAuthInstruction.AddSkill;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([167, 36, 181, 8, 136, 158, 46, 207]),
      ),
      0,
    )
  ) {
    return SkillProofAuthInstruction.CloseProfile;
  }
  throw new SolanaError(
    SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_INSTRUCTION,
    { instructionData: data, programName: "skillProofAuth" },
  );
}

export type ParsedSkillProofAuthInstruction<
  TProgram extends string = "<GggskLBZBfEWv2h27kfGr3vsSniUFyCXCvDzvsMR4T3C>",
> =
  | ({
      instructionType: SkillProofAuthInstruction.InitializeProfile;
    } & ParsedInitializeProfileInstruction<TProgram>)
  | ({
      instructionType: SkillProofAuthInstruction.ReadProfile;
    } & ParsedReadProfileInstruction<TProgram>)
  | ({
      instructionType: SkillProofAuthInstruction.UpdateProfile;
    } & ParsedUpdateProfileInstruction<TProgram>)
  | ({
      instructionType: SkillProofAuthInstruction.AddSkill;
    } & ParsedAddSkillInstruction<TProgram>)
  | ({
      instructionType: SkillProofAuthInstruction.CloseProfile;
    } & ParsedCloseProfileInstruction<TProgram>);

export function parseSkillProofAuthInstruction<TProgram extends string>(
  instruction: Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array>,
): ParsedSkillProofAuthInstruction<TProgram> {
  const instructionType = identifySkillProofAuthInstruction(instruction);
  switch (instructionType) {
    case SkillProofAuthInstruction.InitializeProfile: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: SkillProofAuthInstruction.InitializeProfile,
        ...parseInitializeProfileInstruction(instruction),
      };
    }
    case SkillProofAuthInstruction.ReadProfile: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: SkillProofAuthInstruction.ReadProfile,
        ...parseReadProfileInstruction(instruction),
      };
    }
    case SkillProofAuthInstruction.UpdateProfile: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: SkillProofAuthInstruction.UpdateProfile,
        ...parseUpdateProfileInstruction(instruction),
      };
    }
    case SkillProofAuthInstruction.AddSkill: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: SkillProofAuthInstruction.AddSkill,
        ...parseAddSkillInstruction(instruction),
      };
    }
    case SkillProofAuthInstruction.CloseProfile: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: SkillProofAuthInstruction.CloseProfile,
        ...parseCloseProfileInstruction(instruction),
      };
    }
    default:
      throw new SolanaError(
        SOLANA_ERROR__PROGRAM_CLIENTS__UNRECOGNIZED_INSTRUCTION_TYPE,
        {
          instructionType: instructionType as string,
          programName: "skillProofAuth",
        },
      );
  }
}

export type SkillProofAuthPlugin = {
  accounts: SkillProofAuthPluginAccounts;
  instructions: SkillProofAuthPluginInstructions;
};

export type SkillProofAuthPluginAccounts = {
  userProfile: ReturnType<typeof getUserProfileCodec> &
    SelfFetchFunctions<UserProfileArgs, UserProfile>;
};

export type SkillProofAuthPluginInstructions = {
  initializeProfile: (
    input: InitializeProfileInput,
  ) => ReturnType<typeof getInitializeProfileInstruction> &
    SelfPlanAndSendFunctions;
  readProfile: (
    input: ReadProfileInput,
  ) => ReturnType<typeof getReadProfileInstruction> & SelfPlanAndSendFunctions;
  updateProfile: (
    input: UpdateProfileInput,
  ) => ReturnType<typeof getUpdateProfileInstruction> &
    SelfPlanAndSendFunctions;
  addSkill: (
    input: AddSkillInput,
  ) => ReturnType<typeof getAddSkillInstruction> & SelfPlanAndSendFunctions;
  closeProfile: (
    input: CloseProfileInput,
  ) => ReturnType<typeof getCloseProfileInstruction> & SelfPlanAndSendFunctions;
};

export type SkillProofAuthPluginRequirements = ClientWithRpc<
  GetAccountInfoApi & GetMultipleAccountsApi
> &
  ClientWithTransactionPlanning &
  ClientWithTransactionSending;

export function skillProofAuthProgram() {
  return <T extends SkillProofAuthPluginRequirements>(
    client: T,
  ): Omit<T, "skillProofAuth"> & { skillProofAuth: SkillProofAuthPlugin } => {
    return extendClient(client, {
      skillProofAuth: <SkillProofAuthPlugin>{
        accounts: {
          userProfile: addSelfFetchFunctions(client, getUserProfileCodec()),
        },
        instructions: {
          initializeProfile: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getInitializeProfileInstruction(input),
            ),
          readProfile: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getReadProfileInstruction(input),
            ),
          updateProfile: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getUpdateProfileInstruction(input),
            ),
          addSkill: (input) =>
            addSelfPlanAndSendFunctions(client, getAddSkillInstruction(input)),
          closeProfile: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getCloseProfileInstruction(input),
            ),
        },
      },
    });
  };
}
