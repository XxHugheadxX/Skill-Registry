/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { SKILL_PROOF_AUTH_PROGRAM_ADDRESS } from "../programs";

export const READ_PROFILE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  221, 127, 18, 130, 112, 49, 200, 247,
]);

export function getReadProfileDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    READ_PROFILE_DISCRIMINATOR,
  );
}

export type ReadProfileInstruction<
  TProgram extends string = typeof SKILL_PROOF_AUTH_PROGRAM_ADDRESS,
  TAccountProfile extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountProfile extends string
        ? ReadonlyAccount<TAccountProfile>
        : TAccountProfile,
      TAccountOwner extends string
        ? ReadonlySignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      ...TRemainingAccounts,
    ]
  >;

export type ReadProfileInstructionData = { discriminator: ReadonlyUint8Array };

export type ReadProfileInstructionDataArgs = {};

export function getReadProfileInstructionDataEncoder(): FixedSizeEncoder<ReadProfileInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: READ_PROFILE_DISCRIMINATOR }),
  );
}

export function getReadProfileInstructionDataDecoder(): FixedSizeDecoder<ReadProfileInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getReadProfileInstructionDataCodec(): FixedSizeCodec<
  ReadProfileInstructionDataArgs,
  ReadProfileInstructionData
> {
  return combineCodec(
    getReadProfileInstructionDataEncoder(),
    getReadProfileInstructionDataDecoder(),
  );
}

export type ReadProfileInput<
  TAccountProfile extends string = string,
  TAccountOwner extends string = string,
> = {
  profile: Address<TAccountProfile>;
  owner: TransactionSigner<TAccountOwner>;
};

export function getReadProfileInstruction<
  TAccountProfile extends string,
  TAccountOwner extends string,
  TProgramAddress extends Address = typeof SKILL_PROOF_AUTH_PROGRAM_ADDRESS,
>(
  input: ReadProfileInput<TAccountProfile, TAccountOwner>,
  config?: { programAddress?: TProgramAddress },
): ReadProfileInstruction<TProgramAddress, TAccountProfile, TAccountOwner> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SKILL_PROOF_AUTH_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    profile: { value: input.profile ?? null, isWritable: false },
    owner: { value: input.owner ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("profile", accounts.profile),
      getAccountMeta("owner", accounts.owner),
    ],
    data: getReadProfileInstructionDataEncoder().encode({}),
    programAddress,
  } as ReadProfileInstruction<TProgramAddress, TAccountProfile, TAccountOwner>);
}

export type ParsedReadProfileInstruction<
  TProgram extends string = typeof SKILL_PROOF_AUTH_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    profile: TAccountMetas[0];
    owner: TAccountMetas[1];
  };
  data: ReadProfileInstructionData;
};

export function parseReadProfileInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedReadProfileInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { profile: getNextAccount(), owner: getNextAccount() },
    data: getReadProfileInstructionDataDecoder().decode(instruction.data),
  };
}
