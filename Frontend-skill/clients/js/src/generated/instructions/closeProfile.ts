/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { SKILL_PROOF_AUTH_PROGRAM_ADDRESS } from "../programs";

export const CLOSE_PROFILE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  167, 36, 181, 8, 136, 158, 46, 207,
]);

export function getCloseProfileDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLOSE_PROFILE_DISCRIMINATOR,
  );
}

export type CloseProfileInstruction<
  TProgram extends string = typeof SKILL_PROOF_AUTH_PROGRAM_ADDRESS,
  TAccountProfile extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountProfile extends string
        ? WritableAccount<TAccountProfile>
        : TAccountProfile,
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      ...TRemainingAccounts,
    ]
  >;

export type CloseProfileInstructionData = { discriminator: ReadonlyUint8Array };

export type CloseProfileInstructionDataArgs = {};

export function getCloseProfileInstructionDataEncoder(): FixedSizeEncoder<CloseProfileInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CLOSE_PROFILE_DISCRIMINATOR }),
  );
}

export function getCloseProfileInstructionDataDecoder(): FixedSizeDecoder<CloseProfileInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCloseProfileInstructionDataCodec(): FixedSizeCodec<
  CloseProfileInstructionDataArgs,
  CloseProfileInstructionData
> {
  return combineCodec(
    getCloseProfileInstructionDataEncoder(),
    getCloseProfileInstructionDataDecoder(),
  );
}

export type CloseProfileInput<
  TAccountProfile extends string = string,
  TAccountOwner extends string = string,
> = {
  profile: Address<TAccountProfile>;
  owner: TransactionSigner<TAccountOwner>;
};

export function getCloseProfileInstruction<
  TAccountProfile extends string,
  TAccountOwner extends string,
  TProgramAddress extends Address = typeof SKILL_PROOF_AUTH_PROGRAM_ADDRESS,
>(
  input: CloseProfileInput<TAccountProfile, TAccountOwner>,
  config?: { programAddress?: TProgramAddress },
): CloseProfileInstruction<TProgramAddress, TAccountProfile, TAccountOwner> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SKILL_PROOF_AUTH_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    profile: { value: input.profile ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("profile", accounts.profile),
      getAccountMeta("owner", accounts.owner),
    ],
    data: getCloseProfileInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseProfileInstruction<
    TProgramAddress,
    TAccountProfile,
    TAccountOwner
  >);
}

export type ParsedCloseProfileInstruction<
  TProgram extends string = typeof SKILL_PROOF_AUTH_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    profile: TAccountMetas[0];
    owner: TAccountMetas[1];
  };
  data: CloseProfileInstructionData;
};

export function parseCloseProfileInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCloseProfileInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { profile: getNextAccount(), owner: getNextAccount() },
    data: getCloseProfileInstructionDataDecoder().decode(instruction.data),
  };
}
