/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { SKILL_PROOF_AUTH_PROGRAM_ADDRESS } from "../programs";

export const ADD_SKILL_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  193, 213, 92, 116, 27, 180, 153, 6,
]);

export function getAddSkillDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(ADD_SKILL_DISCRIMINATOR);
}

export type AddSkillInstruction<
  TProgram extends string = typeof SKILL_PROOF_AUTH_PROGRAM_ADDRESS,
  TAccountProfile extends string | AccountMeta<string> = string,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountProfile extends string
        ? WritableAccount<TAccountProfile>
        : TAccountProfile,
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      ...TRemainingAccounts,
    ]
  >;

export type AddSkillInstructionData = {
  discriminator: ReadonlyUint8Array;
  points: number;
};

export type AddSkillInstructionDataArgs = { points: number };

export function getAddSkillInstructionDataEncoder(): FixedSizeEncoder<AddSkillInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["points", getU32Encoder()],
    ]),
    (value) => ({ ...value, discriminator: ADD_SKILL_DISCRIMINATOR }),
  );
}

export function getAddSkillInstructionDataDecoder(): FixedSizeDecoder<AddSkillInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["points", getU32Decoder()],
  ]);
}

export function getAddSkillInstructionDataCodec(): FixedSizeCodec<
  AddSkillInstructionDataArgs,
  AddSkillInstructionData
> {
  return combineCodec(
    getAddSkillInstructionDataEncoder(),
    getAddSkillInstructionDataDecoder(),
  );
}

export type AddSkillInput<
  TAccountProfile extends string = string,
  TAccountAdmin extends string = string,
> = {
  profile: Address<TAccountProfile>;
  admin: TransactionSigner<TAccountAdmin>;
  points: AddSkillInstructionDataArgs["points"];
};

export function getAddSkillInstruction<
  TAccountProfile extends string,
  TAccountAdmin extends string,
  TProgramAddress extends Address = typeof SKILL_PROOF_AUTH_PROGRAM_ADDRESS,
>(
  input: AddSkillInput<TAccountProfile, TAccountAdmin>,
  config?: { programAddress?: TProgramAddress },
): AddSkillInstruction<TProgramAddress, TAccountProfile, TAccountAdmin> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SKILL_PROOF_AUTH_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    profile: { value: input.profile ?? null, isWritable: true },
    admin: { value: input.admin ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("profile", accounts.profile),
      getAccountMeta("admin", accounts.admin),
    ],
    data: getAddSkillInstructionDataEncoder().encode(
      args as AddSkillInstructionDataArgs,
    ),
    programAddress,
  } as AddSkillInstruction<TProgramAddress, TAccountProfile, TAccountAdmin>);
}

export type ParsedAddSkillInstruction<
  TProgram extends string = typeof SKILL_PROOF_AUTH_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    profile: TAccountMetas[0];
    admin: TAccountMetas[1];
  };
  data: AddSkillInstructionData;
};

export function parseAddSkillInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAddSkillInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { profile: getNextAccount(), admin: getNextAccount() },
    data: getAddSkillInstructionDataDecoder().decode(instruction.data),
  };
}
