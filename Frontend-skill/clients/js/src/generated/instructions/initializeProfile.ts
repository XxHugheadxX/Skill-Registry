/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { SKILL_PROOF_AUTH_PROGRAM_ADDRESS } from "../programs";

export const INITIALIZE_PROFILE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([32, 145, 77, 213, 58, 39, 251, 234]);

export function getInitializeProfileDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_PROFILE_DISCRIMINATOR,
  );
}

export type InitializeProfileInstruction<
  TProgram extends string = typeof SKILL_PROOF_AUTH_PROGRAM_ADDRESS,
  TAccountProfile extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountProfile extends string
        ? WritableAccount<TAccountProfile>
        : TAccountProfile,
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeProfileInstructionData = {
  discriminator: ReadonlyUint8Array;
  name: string;
};

export type InitializeProfileInstructionDataArgs = { name: string };

export function getInitializeProfileInstructionDataEncoder(): Encoder<InitializeProfileInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["name", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: INITIALIZE_PROFILE_DISCRIMINATOR }),
  );
}

export function getInitializeProfileInstructionDataDecoder(): Decoder<InitializeProfileInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["name", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getInitializeProfileInstructionDataCodec(): Codec<
  InitializeProfileInstructionDataArgs,
  InitializeProfileInstructionData
> {
  return combineCodec(
    getInitializeProfileInstructionDataEncoder(),
    getInitializeProfileInstructionDataDecoder(),
  );
}

export type InitializeProfileInput<
  TAccountProfile extends string = string,
  TAccountOwner extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  profile: Address<TAccountProfile>;
  owner: TransactionSigner<TAccountOwner>;
  systemProgram?: Address<TAccountSystemProgram>;
  name: InitializeProfileInstructionDataArgs["name"];
};

export function getInitializeProfileInstruction<
  TAccountProfile extends string,
  TAccountOwner extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SKILL_PROOF_AUTH_PROGRAM_ADDRESS,
>(
  input: InitializeProfileInput<
    TAccountProfile,
    TAccountOwner,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeProfileInstruction<
  TProgramAddress,
  TAccountProfile,
  TAccountOwner,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SKILL_PROOF_AUTH_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    profile: { value: input.profile ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("profile", accounts.profile),
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializeProfileInstructionDataEncoder().encode(
      args as InitializeProfileInstructionDataArgs,
    ),
    programAddress,
  } as InitializeProfileInstruction<
    TProgramAddress,
    TAccountProfile,
    TAccountOwner,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeProfileInstruction<
  TProgram extends string = typeof SKILL_PROOF_AUTH_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    profile: TAccountMetas[0];
    owner: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: InitializeProfileInstructionData;
};

export function parseInitializeProfileInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeProfileInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      profile: getNextAccount(),
      owner: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeProfileInstructionDataDecoder().decode(instruction.data),
  };
}
