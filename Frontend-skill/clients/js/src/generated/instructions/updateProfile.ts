/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { SKILL_PROOF_AUTH_PROGRAM_ADDRESS } from "../programs";

export const UPDATE_PROFILE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  98, 67, 99, 206, 86, 115, 175, 1,
]);

export function getUpdateProfileDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_PROFILE_DISCRIMINATOR,
  );
}

export type UpdateProfileInstruction<
  TProgram extends string = typeof SKILL_PROOF_AUTH_PROGRAM_ADDRESS,
  TAccountProfile extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountProfile extends string
        ? WritableAccount<TAccountProfile>
        : TAccountProfile,
      TAccountOwner extends string
        ? ReadonlySignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateProfileInstructionData = {
  discriminator: ReadonlyUint8Array;
  newName: string;
};

export type UpdateProfileInstructionDataArgs = { newName: string };

export function getUpdateProfileInstructionDataEncoder(): Encoder<UpdateProfileInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["newName", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_PROFILE_DISCRIMINATOR }),
  );
}

export function getUpdateProfileInstructionDataDecoder(): Decoder<UpdateProfileInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["newName", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getUpdateProfileInstructionDataCodec(): Codec<
  UpdateProfileInstructionDataArgs,
  UpdateProfileInstructionData
> {
  return combineCodec(
    getUpdateProfileInstructionDataEncoder(),
    getUpdateProfileInstructionDataDecoder(),
  );
}

export type UpdateProfileInput<
  TAccountProfile extends string = string,
  TAccountOwner extends string = string,
> = {
  profile: Address<TAccountProfile>;
  owner: TransactionSigner<TAccountOwner>;
  newName: UpdateProfileInstructionDataArgs["newName"];
};

export function getUpdateProfileInstruction<
  TAccountProfile extends string,
  TAccountOwner extends string,
  TProgramAddress extends Address = typeof SKILL_PROOF_AUTH_PROGRAM_ADDRESS,
>(
  input: UpdateProfileInput<TAccountProfile, TAccountOwner>,
  config?: { programAddress?: TProgramAddress },
): UpdateProfileInstruction<TProgramAddress, TAccountProfile, TAccountOwner> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SKILL_PROOF_AUTH_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    profile: { value: input.profile ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("profile", accounts.profile),
      getAccountMeta("owner", accounts.owner),
    ],
    data: getUpdateProfileInstructionDataEncoder().encode(
      args as UpdateProfileInstructionDataArgs,
    ),
    programAddress,
  } as UpdateProfileInstruction<
    TProgramAddress,
    TAccountProfile,
    TAccountOwner
  >);
}

export type ParsedUpdateProfileInstruction<
  TProgram extends string = typeof SKILL_PROOF_AUTH_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    profile: TAccountMetas[0];
    owner: TAccountMetas[1];
  };
  data: UpdateProfileInstructionData;
};

export function parseUpdateProfileInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateProfileInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { profile: getNextAccount(), owner: getNextAccount() },
    data: getUpdateProfileInstructionDataDecoder().decode(instruction.data),
  };
}
