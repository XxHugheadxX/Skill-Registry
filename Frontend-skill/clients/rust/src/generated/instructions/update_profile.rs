//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshSerialize;
use borsh::BorshDeserialize;

pub const UPDATE_PROFILE_DISCRIMINATOR: [u8; 8] = [98, 67, 99, 206, 86, 115, 175, 1];

/// Accounts.
#[derive(Debug)]
pub struct UpdateProfile {
      
              
          pub profile: solana_address::Address,
          
              
          pub owner: solana_address::Address,
      }

impl UpdateProfile {
  pub fn instruction(&self, args: UpdateProfileInstructionArgs) -> solana_instruction::Instruction {
    self.instruction_with_remaining_accounts(args, &[])
  }
  #[allow(clippy::arithmetic_side_effects)]
  #[allow(clippy::vec_init_then_push)]
  pub fn instruction_with_remaining_accounts(&self, args: UpdateProfileInstructionArgs, remaining_accounts: &[solana_instruction::AccountMeta]) -> solana_instruction::Instruction {
    let mut accounts = Vec::with_capacity(2+ remaining_accounts.len());
                            accounts.push(solana_instruction::AccountMeta::new(
            self.profile,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.owner,
            true
          ));
                      accounts.extend_from_slice(remaining_accounts);
    let mut data = UpdateProfileInstructionData::new().try_to_vec().unwrap();
          let mut args = args.try_to_vec().unwrap();
      data.append(&mut args);
    
    solana_instruction::Instruction {
      program_id: crate::SKILL_PROOF_AUTH_ID,
      accounts,
      data,
    }
  }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
 pub struct UpdateProfileInstructionData {
            discriminator: [u8; 8],
            }

impl UpdateProfileInstructionData {
  pub fn new() -> Self {
    Self {
                        discriminator: [98, 67, 99, 206, 86, 115, 175, 1],
                                }
  }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
    borsh::to_vec(self)
  }
  }

impl Default for UpdateProfileInstructionData {
  fn default() -> Self {
    Self::new()
  }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
 pub struct UpdateProfileInstructionArgs {
                  pub new_name: String,
      }

impl UpdateProfileInstructionArgs {
  pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
    borsh::to_vec(self)
  }
}


/// Instruction builder for `UpdateProfile`.
///
/// ### Accounts:
///
                ///   0. `[writable]` profile
                ///   1. `[signer]` owner
#[derive(Clone, Debug, Default)]
pub struct UpdateProfileBuilder {
            profile: Option<solana_address::Address>,
                owner: Option<solana_address::Address>,
                        new_name: Option<String>,
        __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl UpdateProfileBuilder {
  pub fn new() -> Self {
    Self::default()
  }
            #[inline(always)]
    pub fn profile(&mut self, profile: solana_address::Address) -> &mut Self {
                        self.profile = Some(profile);
                    self
    }
            #[inline(always)]
    pub fn owner(&mut self, owner: solana_address::Address) -> &mut Self {
                        self.owner = Some(owner);
                    self
    }
                    #[inline(always)]
      pub fn new_name(&mut self, new_name: String) -> &mut Self {
        self.new_name = Some(new_name);
        self
      }
        /// Add an additional account to the instruction.
  #[inline(always)]
  pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
    self.__remaining_accounts.push(account);
    self
  }
  /// Add additional accounts to the instruction.
  #[inline(always)]
  pub fn add_remaining_accounts(&mut self, accounts: &[solana_instruction::AccountMeta]) -> &mut Self {
    self.__remaining_accounts.extend_from_slice(accounts);
    self
  }
  #[allow(clippy::clone_on_copy)]
  pub fn instruction(&self) -> solana_instruction::Instruction {
    let accounts = UpdateProfile {
                              profile: self.profile.expect("profile is not set"),
                                        owner: self.owner.expect("owner is not set"),
                      };
          let args = UpdateProfileInstructionArgs {
                                                              new_name: self.new_name.clone().expect("new_name is not set"),
                                    };
    
    accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
  }
}

  /// `update_profile` CPI accounts.
  pub struct UpdateProfileCpiAccounts<'a, 'b> {
          
                    
              pub profile: &'b solana_account_info::AccountInfo<'a>,
                
                    
              pub owner: &'b solana_account_info::AccountInfo<'a>,
            }

/// `update_profile` CPI instruction.
pub struct UpdateProfileCpi<'a, 'b> {
  /// The program to invoke.
  pub __program: &'b solana_account_info::AccountInfo<'a>,
      
              
          pub profile: &'b solana_account_info::AccountInfo<'a>,
          
              
          pub owner: &'b solana_account_info::AccountInfo<'a>,
            /// The arguments for the instruction.
    pub __args: UpdateProfileInstructionArgs,
  }

impl<'a, 'b> UpdateProfileCpi<'a, 'b> {
  pub fn new(
    program: &'b solana_account_info::AccountInfo<'a>,
          accounts: UpdateProfileCpiAccounts<'a, 'b>,
              args: UpdateProfileInstructionArgs,
      ) -> Self {
    Self {
      __program: program,
              profile: accounts.profile,
              owner: accounts.owner,
                    __args: args,
          }
  }
  #[inline(always)]
  pub fn invoke(&self) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(&[], &[])
  }
  #[inline(always)]
  pub fn invoke_with_remaining_accounts(&self, remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
  }
  #[inline(always)]
  pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
  }
  #[allow(clippy::arithmetic_side_effects)]
  #[allow(clippy::clone_on_copy)]
  #[allow(clippy::vec_init_then_push)]
  pub fn invoke_signed_with_remaining_accounts(
    &self,
    signers_seeds: &[&[&[u8]]],
    remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]
  ) -> solana_program_error::ProgramResult {
    let mut accounts = Vec::with_capacity(2+ remaining_accounts.len());
                            accounts.push(solana_instruction::AccountMeta::new(
            *self.profile.key,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.owner.key,
            true
          ));
                      remaining_accounts.iter().for_each(|remaining_account| {
      accounts.push(solana_instruction::AccountMeta {
          pubkey: *remaining_account.0.key,
          is_signer: remaining_account.1,
          is_writable: remaining_account.2,
      })
    });
    let mut data = UpdateProfileInstructionData::new().try_to_vec().unwrap();
          let mut args = self.__args.try_to_vec().unwrap();
      data.append(&mut args);
    
    let instruction = solana_instruction::Instruction {
      program_id: crate::SKILL_PROOF_AUTH_ID,
      accounts,
      data,
    };
    let mut account_infos = Vec::with_capacity(3 + remaining_accounts.len());
    account_infos.push(self.__program.clone());
                  account_infos.push(self.profile.clone());
                        account_infos.push(self.owner.clone());
              remaining_accounts.iter().for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

    if signers_seeds.is_empty() {
      solana_cpi::invoke(&instruction, &account_infos)
    } else {
      solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
    }
  }
}

/// Instruction builder for `UpdateProfile` via CPI.
///
/// ### Accounts:
///
                ///   0. `[writable]` profile
                ///   1. `[signer]` owner
#[derive(Clone, Debug)]
pub struct UpdateProfileCpiBuilder<'a, 'b> {
  instruction: Box<UpdateProfileCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> UpdateProfileCpiBuilder<'a, 'b> {
  pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
    let instruction = Box::new(UpdateProfileCpiBuilderInstruction {
      __program: program,
              profile: None,
              owner: None,
                                            new_name: None,
                    __remaining_accounts: Vec::new(),
    });
    Self { instruction }
  }
      #[inline(always)]
    pub fn profile(&mut self, profile: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.profile = Some(profile);
                    self
    }
      #[inline(always)]
    pub fn owner(&mut self, owner: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.owner = Some(owner);
                    self
    }
                    #[inline(always)]
      pub fn new_name(&mut self, new_name: String) -> &mut Self {
        self.instruction.new_name = Some(new_name);
        self
      }
        /// Add an additional account to the instruction.
  #[inline(always)]
  pub fn add_remaining_account(&mut self, account: &'b solana_account_info::AccountInfo<'a>, is_writable: bool, is_signer: bool) -> &mut Self {
    self.instruction.__remaining_accounts.push((account, is_writable, is_signer));
    self
  }
  /// Add additional accounts to the instruction.
  ///
  /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
  /// and a `bool` indicating whether the account is a signer or not.
  #[inline(always)]
  pub fn add_remaining_accounts(&mut self, accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]) -> &mut Self {
    self.instruction.__remaining_accounts.extend_from_slice(accounts);
    self
  }
  #[inline(always)]
  pub fn invoke(&self) -> solana_program_error::ProgramResult {
    self.invoke_signed(&[])
  }
  #[allow(clippy::clone_on_copy)]
  #[allow(clippy::vec_init_then_push)]
  pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
          let args = UpdateProfileInstructionArgs {
                                                              new_name: self.instruction.new_name.clone().expect("new_name is not set"),
                                    };
        let instruction = UpdateProfileCpi {
        __program: self.instruction.__program,
                  
          profile: self.instruction.profile.expect("profile is not set"),
                  
          owner: self.instruction.owner.expect("owner is not set"),
                          __args: args,
            };
    instruction.invoke_signed_with_remaining_accounts(signers_seeds, &self.instruction.__remaining_accounts)
  }
}

#[derive(Clone, Debug)]
struct UpdateProfileCpiBuilderInstruction<'a, 'b> {
  __program: &'b solana_account_info::AccountInfo<'a>,
            profile: Option<&'b solana_account_info::AccountInfo<'a>>,
                owner: Option<&'b solana_account_info::AccountInfo<'a>>,
                        new_name: Option<String>,
        /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
  __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}

